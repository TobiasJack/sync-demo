<UserControl x:Class="SyncDemo.WpfApp.Views.SyncItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <DataGrid ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  AlternatingRowBackground="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="250"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Beschreibung" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Erstellt" Binding="{Binding CreatedAt, StringFormat='dd.MM.yyyy HH:mm:ss'}" Width="150"/>
                <DataGridTextColumn Header="Aktualisiert" Binding="{Binding ModifiedAt, StringFormat='dd.MM.yyyy HH:mm:ss'}" Width="150"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
